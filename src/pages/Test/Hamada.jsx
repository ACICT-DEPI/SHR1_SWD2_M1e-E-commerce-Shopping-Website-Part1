import React from "react";
import { useSelector } from "react-redux";

const Hamada = () => {
  // const getProducts = useSelector((state) => state.allProducts);
  // getProducts.map((product) => {
  //   console.log(product);
  // });

  // const getUsers = useSelector((state) => state.allUsers);
  // getUsers.map((user) => {
  //   console.log(user);
  // });

  // const getOrders = useSelector((state) => state.allOrders);
  // getOrders.map((order) => {
  //   console.log(order);
  // });

  // const getComments = useSelector((state) => state.allComments);
  // getComments.map((comment) => {
  //   console.log(comment);
  // });

  // const getReviews = useSelector((state) => state.allReviews);
  // getReviews.map((review) => {
  //   console.log(review);
  // });

  return (
    <div>
      <div>Hello Hamada</div>
      <p>Test Zapier</p>
      <p>Test Zapier 2</p>
      <p>Test Zapier 3</p>
      <p>Test Zapier 4</p>
    </div>
  );
};

export default Hamada;
